#!/usr/bin/env bash

set -e

original=$(xdotool getactivewindow)
browser=$(xdotool search --onlyvisible --name Chrome | head -1)

xdotool windowactivate --sync "$browser"
xdotool key --window "$browser" F5
xdotool windowactivate "$original"
